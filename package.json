{
  "name": "remitready-ca",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "npm run validate:rates && npm test && vite build",
    "dev": "vite --host 0.0.0.0 --port 4173",
    "lint": "eslint .",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "test": "node --test tests/*.test.js",
    "validate:rates": "node scripts/validate-rates.js"
  },
  "dependencies": {
    "alpinejs": "^3.14.0",
    "tailwindcss": "^4.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/vite": "^4.3.0",
    "eslint": "^10.4.0",
    "vite": "^8.0.14"
  }
}
